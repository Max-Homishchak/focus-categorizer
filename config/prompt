Ok, lets continue this way, here how V2 will look like:
1) Preserve the current functionality, but change the implementation approach.
2) Run the categorization prompt in parallel across X instances.
3) Add offset support for categorization:
  3.1) If the process is interrupted, the system must fetch or take already fetched all messages from the channel filter out messages that have been already categorized and continue only with the remaining unprocessed items - this is the only flow I think that will work in case of parallel execution, where 0-10 could be interrupted, while 11-20 processed
  3.2) If new questionnaires are added, the system must detect the latest questionnaire that has already been processed and continue from that point forward.
  3.3) If the categorization prompt itself changes, the offset must be ignored and the entire flow must be rerun from the beginning. My only idea to implement such logic is to calculate the hash of the prompt, if it changes - then recategorize whole flow (confirm with user first on UI, at the moment he updates prompt)
4) Support self-hosting UI by the use, web app will be added in the next release.
5) Implement semantic search, available to the user after the categorization flow has been run.
   5.1) Search flow requirements:
      5.1.1) Use top-k = 25 for retrieval.
      5.1.2) Apply LLM-based reranking on the retrieved results.
      5.1.3) Based on the final reranked list of users, filter out all other users on the main categorization page.
      5.1.4) Add a Reset option to restore the full user list.
      5.1.5The search UI should be shown as a chat icon in the bottom-right corner.
6) fallback behavior:
  6.1) If no user is found for a given query, show a message explaining that no matching people could be found in the community at the moment.